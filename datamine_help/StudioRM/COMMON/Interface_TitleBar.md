# The Title Bar

The Title Bar displays the application name, and if loaded, the current project file name. 

Double-clicking the title bar maximizes the screen if not already maximized, or revert it to a sizeable window if it is.