# move-next-section ("mns")

See this command in the [**command table**.](<COMMAND%20TABLE_M.md#move-next-section>)

To access this command:

  * Activate the 3D **View** ribbon and select **Sections >> Next**.
  * If using a keyboard, press **End**.

  * Using the **[command line](<../COMMON/Command_Toolbar.md>)** , enter "move-next-section"

## Command Overview

Activate the next section definition of a loaded section definition table.

Command Steps:

  1. Ensure a section definition table is loaded.
  2. Activate a **3D** view.
  3. Run the command.

The next section definition in the loaded table is activated and the 3D view updates, if data is rendered that is reliant on a particular section definition. For example, clipping may be applied differently after changing sections, or if the view is locked to the section, it updates automatically to be orthogonal to the latest definition.

Related topics and activities

  * [move-previous-section ("mps")](<move-previous-section.md>)

  * [move-clip-section-backward ("msb")](<move-clip-section-backward.md>)

  * [move-clip-section-forward ("msf")](<move-clip-section-forward.md>)